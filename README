I just figured out how to do multiple SSH keys with github.  I found the article here: http://github.com/guides/multiple-github-accounts

Basically:

* create a second public/private key pair:
  ssh-keygen
  named the new file id_rsa2

* add a host in my .ssh/config:
  Host github-urug
    HostName github.com
    User git
    IdentityFile /Users/davidrichards/.ssh/id_rsa2

* git clone git@github-urug:urug/strategery.git  strategery 
  instead of  git clone git@github.com:urug/strategery.git  strategery

Another way may be to be a collaborator with my normal github account.  I don't know the details of this