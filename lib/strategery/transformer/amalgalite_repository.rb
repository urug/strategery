module SVM #:nodoc:
  module Transformer #:nodoc:
    class AmalgaliteRepository
      # Now, need to make an interface that makes sense.  The idea is to use either a :memory: or actual SQLite3 database to store the data.  It depends if we need some really fast interaction here.
    end
  end
end